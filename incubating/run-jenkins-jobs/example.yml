version: '1.0'
steps:
  RunJenkins:
    title: Triggering Jenkins Job
    image: quay.io/codefresh/run-jenkins-jobs
    environment:
    - JENKINS_URL=http://<jenkins host>:<jenkins port>
    - JENKINS_USER=<jenkins user name>
    - JENKINS_TOKEN=<jenkins token>
    - JENKINS_JOB=<jenkins job name>